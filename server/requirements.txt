# The oemer package itself (pip install -e .. from the repo root) brings the
# pipeline dependencies, including the pinned opencv-python-headless<5.
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9

# Tests
pytest>=8.0
httpx>=0.27
